<UserControl
    x:Class="DashboardPro.App.Modules.NotesModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="6">
        <Grid ColumnSpacing="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewNoteBox" PlaceholderText="Быстрая заметка…" AcceptsReturn="True" MaxHeight="90" />
            <Button Grid.Column="1" VerticalAlignment="Bottom" Click="OnAddClick"
                    ToolTipService.ToolTip="Сохранить заметку">
                <FontIcon Glyph="&#xE74E;" FontSize="12" />
            </Button>
        </Grid>
        <StackPanel x:Name="ListPanel" Spacing="6" />
    </StackPanel>
</UserControl>
