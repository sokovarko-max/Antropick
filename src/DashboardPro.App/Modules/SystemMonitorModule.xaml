<UserControl
    x:Class="DashboardPro.App.Modules.SystemMonitorModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="6">
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="44" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="44" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="CPU" FontSize="12" VerticalAlignment="Center" />
            <ProgressBar x:Name="CpuBar" Grid.Column="1" Minimum="0" Maximum="100" VerticalAlignment="Center" />
            <TextBlock x:Name="CpuText" Grid.Column="2" FontSize="12" VerticalAlignment="Center"
                       HorizontalAlignment="Right" />
        </Grid>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="44" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="44" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="RAM" FontSize="12" VerticalAlignment="Center" />
            <ProgressBar x:Name="RamBar" Grid.Column="1" Minimum="0" Maximum="100" VerticalAlignment="Center" />
            <TextBlock x:Name="RamText" Grid.Column="2" FontSize="12" VerticalAlignment="Center"
                       HorizontalAlignment="Right" />
        </Grid>
        <TextBlock x:Name="DetailsText" Style="{StaticResource SecondaryTextStyle}" />
        <TextBlock x:Name="NetText" Style="{StaticResource SecondaryTextStyle}" />
    </StackPanel>
</UserControl>
