<UserControl
    x:Class="DashboardPro.App.Controls.ModuleHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource ModuleCardStyle}">
        <StackPanel Spacing="8">
            <Grid ColumnSpacing="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <FontIcon x:Name="Icon" Grid.Column="0" FontSize="13"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="TitleText" Grid.Column="1" Style="{StaticResource ModuleTitleStyle}" />

                <Button x:Name="SettingsButton" Grid.Column="2" Style="{StaticResource ModuleIconButtonStyle}"
                        Click="OnSettingsClick" ToolTipService.ToolTip="Настройки модуля">
                    <FontIcon Glyph="&#xE713;" FontSize="12" />
                </Button>
                <Button x:Name="CollapseButton" Grid.Column="3" Style="{StaticResource ModuleIconButtonStyle}"
                        Click="OnCollapseClick" ToolTipService.ToolTip="Свернуть">
                    <FontIcon x:Name="CollapseIcon" Glyph="&#xE70E;" FontSize="12" />
                </Button>
                <Button x:Name="CloseButton" Grid.Column="4" Style="{StaticResource ModuleIconButtonStyle}"
                        Click="OnCloseClick" ToolTipService.ToolTip="Убрать с панели">
                    <FontIcon Glyph="&#xE711;" FontSize="12" />
                </Button>
            </Grid>

            <ContentPresenter x:Name="Body" />
        </StackPanel>
    </Border>
</UserControl>
